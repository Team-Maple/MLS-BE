spring.application.name: maple-api
server:
  port: 8080
  shutdown: graceful

springdoc:
  use-fqn: true
  swagger-ui:
    path: /swagger-ui-maple/index.html
    disable-swagger-default-url: true

spring:
  lifecycle:
    timeout-per-shutdown-phase: 5s
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        default_batch_fetch_size: 1000
        order_updates: true
        order_inserts: true
        jdbc:
          batch_size: 1000

management:
  server.port: 8080
  endpoints.web.exposure.include: info, health

jwt:
  secret: JTh4bhwwq69/Cj9gDLDXh8Riktoi9ndjD5T0cfHeSWkyC1aE7Xk2XpcDxwbk3Yyotj9d007VxNQuOYMjRoXMzg==
---
spring:
  config:
    activate:
      on-profile: local
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/maple_db
    username: maple_username
    password: maple_password
  jpa:
    hibernate:
      ddl-auto: update
logging:
  level:
    com.maple.api: DEBUG
---
spring:
  config:
    activate:
      on-profile: prod
  datasource:
    url: jdbc:mysql://mapledb.c1w8cws8cmvy.ap-northeast-2.rds.amazonaws.com:3306/mapledb # 환경변수로 바꾸셔도 됩니다.
    username: maple
    password: Mapleland12!! # 환경변수로 바꾸셔도 됩니다.

    driver-class-name: org.postgresql.Driver