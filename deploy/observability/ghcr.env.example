# Install as /opt/mapleland/ghcr.env, owned by root:root with mode 0600.
# The deployment script accepts exactly these two non-empty, whitespace-free keys.
GHCR_USER=replace-with-ghcr-username
GHCR_TOKEN=replace-with-ghcr-read-packages-token
